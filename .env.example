# VLJ HRMS — Database Configuration (Phase 2)
# Copy this file to .env and fill in your MySQL credentials before running migrations.

# MySQL connection string for Prisma
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://root:Tharunreddy%405523@localhost:3306/vlj_hrms"
# If password contains special chars (@, #, %, etc.) URL-encode them.
# Example: Tharunreddy@5523 → Tharunreddy%405523

# Phase 3 — Express.js Backend (not used yet)
# JWT_SECRET="your-jwt-secret-key"
# JWT_EXPIRES_IN="7d"
# PORT=5000
